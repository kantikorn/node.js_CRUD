<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Books</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.0.0/dist/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
</head>
<body class="container" style="margin-top: 50px;">
    <% if(messages.error) { %>
        <div class="alert alert-danger" role="alert"><%= messages.error %></div>
    <% } %>

    
    <% if(messages.success) { %>
        <div class="alert alert-success" role="alert"><%= messages.success %></div>
    <% } %>

    <div class="card">
        <div class="card-header">
            <ul class="nav nav-pills w-100">
                <li class="nav-link">Books</li>
                <li class="nav-link ml-auto" >
                    <a href="/books/add" class="nav-link active">Add Books</a>
                </li>
            </ul>
        </div>

        <div class="card-body">
            <% if(data.length) { %>
                <table class="table">
                    <thead>
                        <tr>
                            <th scope="col">#</th>
                            <th scope="col">Name</th>
                            <th scope="col">Author</th>
                            <th scope="col">Action</th>
                        </tr>
                    </thead>

                    <tbody>
                        
                       <% for(let i = 0; i < data.length; i++)  { %>
                            <tr>
                                <th scope="rows"><%= data[i].id %></th>
                                <td><%= data[i].name %></td>
                                <td><%= data[i].author %></td>

                                <td>
                                    <a href="../books/edit/<%= data[i].id %>" class="btn btn-warning edit">Edit</a>
                                    <a href="../books/delete/<%= data[i].id %>" onclick="return alert('Are you sure Delete !')" class="btn btn-danger delete">delete</a>
                                </td>
                            </tr>
                        <% } %>
                    </tbody>
                </table>
            <% } %>

            <% if(!data.length) { %>
                <p class="text-center">No Books Found</p>
           <% } %>
        </div>
    </div>

</body>
</html>